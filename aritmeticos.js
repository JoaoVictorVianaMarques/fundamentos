const [a, b, c, d] = [4, 5, 8, 10]

const soma = a + b + c + d
const subtracao = d - c
const multiplicacao = a * c
const divisao = d / a 
const mudulo = b % 2 

console.log(soma, subtracao, multiplicacao, divisao, mudulo)